import type { SVGProps } from 'react';

// This is a placeholder as the actual SVG content for "/LogoFull~640834.svg" was not provided.
// In a real scenario, the SVG code would be placed here.
// For now, using a generic bank icon.
import { Landmark } from 'lucide-react';

export function IdfcLogo(props: SVGProps<SVGSVGElement>) {
  return <Landmark {...props} />;
}
